server {
    listen 8080;
    root /usr/share/nginx/html/;
    index index.html;
    error_page 403 404 @error4xx;
    location /stakater-cloud-orchestrator/ {
        rewrite ^/stakater-cloud-orchestrator/(.*)$ /$1 last;
    }
    location = /stakater-cloud-orchestrator {
        return 301 /stakater-cloud-orchestrator/;
    }
    location @error4xx {
        rewrite ^/([^/]*) /$1/404.html last;
    }
    # redirects issued by nginx will be relative
    absolute_redirect off;
}
